# SECURITY: Copy this to .env and fill with actual values
# NEVER commit .env file to version control!

# Required: NocoDB API Token
NOCODB_TOKEN=your_actual_nocodb_token_here

# Required: Flask Secret Key (generate with: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=generate_a_secure_random_key_here

# Optional: Override default settings
NOCODB_BASE=https://your-nocodb-instance.com/api/v2

# Security Notes:
# 1. Always use HTTPS in production
# 2. Set strong, unique passwords
# 3. Rotate API tokens regularly
# 4. Monitor logs for suspicious activity
# 5. Keep dependencies updated
